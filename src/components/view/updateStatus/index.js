import React, { Component } from 'react'
import { Text, View } from 'react-native'
import UpdateStatus from '../home/updateStatus';

export default class UpdateStatusView extends Component {
    render() {
        return (
            <View>
                <UpdateStatus />
            </View>
        )
    }
}
